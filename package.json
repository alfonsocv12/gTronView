{
  "name": "gTronView",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "build-x86": "electron-packager . gTronView --darwin --arch=x64 --icon=assets/icons/icon.icns --prune=true",
    "build-arm": "electron-packager . gTronView --darwin --arch=arm64 --icon=assets/icons/icon.icns --prune=true"
  },
  "devDependencies": {
    "electron": "^17.0.0",
    "electron-packager": "^15.4.0"
  },
  "dependencies": {
    "@electron/remote": "^2.0.4"
  }
}
